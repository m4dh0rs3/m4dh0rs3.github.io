# The base URL of the site; the only required configuration variable.
base_url = "https://m4dh0rs3.github.io"

# The site title and description; used in feeds by default.
title = "Benedikt Schöps"
description = "My Homepage: Portfolio, Blog, Projects"

# The default language; used in feeds.
default_language = "en"

# The additional languages for the site.
languages = [
    { code = "de" }
]

# Configuration of the Markdown rendering
[markdown]

# When set to "true", all code blocks are highlighted.
highlight_code = true

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "ascetic-white"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Optional translation object. Keys should be language codes.
# Optional translation object. The key if present should be a language code.
[translations]
[translations.de]

description = "Meine Internetpräsenz: Portfolio, Blog, Projekte"